# Implementation Plans

Generated by the improve skill on 2026-06-11, against root commit `28e0468` and
`testeiya` submodule commit `81696d8`. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

> **Submodule note.** `testeiya/` is a **git submodule** (its own repo). Plans
> whose in-scope files live under `testeiya/` must be committed **inside the
> submodule** (`cd testeiya && git ...`); the root repo then records the new
> submodule pointer. Drift checks for those plans run inside `testeiya/` against
> the submodule SHA, not the root SHA. Each plan says which repo it touches.

## Execution order & status

| Plan | Title | Priority | Effort | Repo | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Verification baseline: `bun test` + workspace-classifier tests | P1 | S | testeiya | — | DONE |
| 002 | Propagate the real `isError` flag for tool output | P1 | S | both | — | DONE |
| 003 | Remove four unused root dependencies | P1 | S | root | — | DONE |
| 004 | Fix the `testeiya` build + add `typecheck` scripts | P1 | S | both | — | DONE |
| 005 | Validate `postMessage` origin in the host bridge | P1 | S | root | — | DONE |
| 006 | Make the session store atomic + in-memory cached | P1 | M | testeiya | — | DONE |
| 007 | Gate `/api/*` + the agent WS behind auth; lock down `mcp/add` | P1 | M | testeiya | — | DONE |
| 008 | Harden the path sandbox against symlink escape (+ tests) | P2 | S | testeiya | 001 | DONE |
| 009 | Characterization tests for the SSRF proxy whitelist | P2 | S | testeiya | 001 | DONE |
| 010 | Guard streaming deltas by message id | P2 | M | root | — | DONE |
| 011 | Fix the stuck "Restoring…" spinner | P2 | S | root | — | DONE |
| 012 | Investigate + fix the global `TESTOMATIO` token bleed | P2 | M | testeiya | 001 | BLOCKED (Branch B: no SDK per-session shell env; partial fix landed — single-token guard + documented; full isolation needs SDK feature) |
| 013 | Memoize the streaming message list | P2 | M | root | 002 | DONE |
| 014 | Pin `@testomatio/skills` to an immutable ref | P2 | S | testeiya | — | DONE |
| 015 | Delete the dead legacy `server.ts` | P3 | S | testeiya | — | DONE |
| 016 | Consolidate JSON read-modify-write into a shared util | P3 | M | testeiya | 006 | DONE |
| 017 | Prune dead `ai-elements` components + orphaned deps | P3 | M | root | 003 | REJECTED (maintainer keeps the unused components for potential future use; prune reverted — files, the 3 deps, and tsconfig excludes restored) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **008, 009, 012** depend on **001** — they add `bun test` suites and need the
  test runner wired into `testeiya/package.json` first (001 adds the `test`
  script). If you run them before 001, add the `test` script yourself per 001.
- **013** depends on **002** — both edit the per-message render path and the
  tool-state types; doing 002 first avoids a merge conflict and lets 013 rely on
  the explicit `isError`/`state` field.
- **016** depends on **006** — 006 introduces the atomic write helper; 016
  generalizes it across the other JSON stores. If 006 hasn't landed, 016 should
  create the helper itself.
- **017** depends on **003** — 003 removes the unambiguously-unused deps; 017 is
  the riskier follow-up that deletes dead component files and then prunes the
  deps only those files anchored.

## Recommended batching

1. **Land first (P1, low-risk, unblocks the rest):** 001, 003, 004, 002, 005.
2. **Core reliability/security (P1, M):** 006, 007.
3. **P2 hardening & correctness:** 008, 009, 010, 011, 012, 013, 014.
4. **P3 cleanup:** 015, 016, 017.

## Findings considered and rejected (do not re-audit)

- **Static-file path containment** (`testeiya/src/app-server.ts:244-259`,
  `serveStatic`): not currently exploitable — `new URL(req.url).pathname`
  already normalizes `../` before `join`, and encoded `%2e/%2f` stay literal.
  Hardening-only; folded into 008's maintenance note rather than its own plan.
- **`dangerouslySetInnerHTML` in `components/ai-elements/schema-display.tsx:106`**:
  the component is imported by nothing (latent sink, not a live XSS). It will be
  removed by 017's dead-component sweep; no separate plan.
- **`app/layout.tsx:44` `dangerouslySetInnerHTML`**: a build-time-constant theme
  script with no dynamic input — safe by design.
- **The cold-load effect chain in `app/page.tsx`**: the four cold-load effects
  fan out in parallel and are each guarded; not a waterfall. Not worth changing.
